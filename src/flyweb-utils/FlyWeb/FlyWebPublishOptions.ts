declare type FlyWebPublishOptions = {[name: string]: string};   // URL to user interface. Can be different server. Makes
                                                                // endpoint show up in browser's "local services" UI.
                                                                // If relative, resolves against the root of the server.
export {
    FlyWebPublishOptions
}

